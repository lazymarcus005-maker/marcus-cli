## Agent skills

### Issue tracker

Issues and specs live in GitHub repository `lazymarcus005-maker/marcus-cli`; use `gh --repo lazymarcus005-maker/marcus-cli`. See `docs/agents/issue-tracker.md`.

### Triage labels

Uses the default labels: `needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, and `wontfix`. See `docs/agents/triage-labels.md`.

### Domain docs

This is a single-context repo. Root `CONTEXT.md` and `docs/adr/` do not exist yet; the `/domain-modeling` skill creates them lazily when real terms or decisions get resolved, so proceed silently while they are absent. See `docs/agents/domain.md`.
