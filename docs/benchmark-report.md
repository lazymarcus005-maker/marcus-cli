# Paired Benchmark Report — Loopback Fixture

Status: **recorded controlled paired run on the deterministic loopback provider; no live-endpoint or reference-hardware measurement exists.**

This report records the first controlled paired Macus-versus-unmodified-Pi
benchmark run produced by the harness in `src/workflow/benchmark.ts`. It was
generated by `scripts/run-paired-benchmark.ts` against a deterministic
loopback OpenAI-compatible SSE provider. The "model" is a synthetic fixture
that performs one bounded shell edit; the run proves the paired machinery
records real observations end to end. It is **not** a live-endpoint
compatibility or performance result, and it must not be read as one.

## Run identity

- Report file: `docs/benchmark/loopback-paired-report.json` (raw observations and distributions)
- Benchmark ID: `c8e641ba-fbec-4a82-9bb4-ac8fef737d30`
- Recorded at: 2026-09-19T16:12:49.570Z (local +0700)
- Runtime: Node.js v24.21.0 (the pinned runtime), macOS arm64 (Darwin 25.6.0)
- Code snapshot: commit `362f803` (`Cover compaction and migration crash boundaries in recovery tests`)
- Endpoint: loopback OpenAI-compatible SSE fixture on `127.0.0.1`; no external state was contacted
- Scenario: `loopback-bash-append-edit` — append a marker comment to `app.ts` via the shell
- Starting revision: `75a75cae9ad8994b5c3eb26321add823e4b6d924` (identical across all workspaces; fixed author/dates)
- Prompt SHA-256: `3663dc666605bf83…` (identical for every run; see raw report)
- Generation settings: `{"temperature":0}`; context limit 16,384 tokens; output limit 256 tokens
- Condition: repository cache `cold` (fresh committed workspace per run), provider cache `unknown` (loopback has no cache semantics)
- Repetitions: 3 pairs, order-alternated (Pi first in repetitions 0 and 2, Macus first in repetition 1)

## Correctness first

- Paired comparisons: 3; regressions: none; unknown comparisons: none
- Harness status: `no-regression-observed`
- Test oracle (real check, not a stub): `app.ts` contains the marker and
  `git status --porcelain` shows exactly `app.ts` modified — passed for all 6 runs
- Recovery oracle: same worktree-integrity check — passed for all 6 runs

## Metrics (per-run raw data is in the JSON report)

| System | Rep | Input tokens | Cached | Uncached | Output | Tool calls | Wall ms | Peak context |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| unmodified_pi | 0 | 41 | 11 | 30 | 12 | 1 | 66 | 24 |
| macus | 0 | 41 | 11 | 30 | 12 | 1 | 98 | 24 |
| macus | 1 | 41 | 11 | 30 | 12 | 1 | 83 | 24 |
| unmodified_pi | 1 | 41 | 11 | 30 | 12 | 1 | 6 | 24 |
| unmodified_pi | 2 | 41 | 11 | 30 | 12 | 1 | 11 | 24 |
| macus | 2 | 41 | 11 | 30 | 12 | 1 | 91 | 24 |

Distributions (median): wall time unmodified_pi 10.8 ms, macus 91.1 ms. The
Macus kernel adds durable-state, policy, and budget work per run, and the
unmodified-Pi times are dominated by loopback latency; these values are
recorded observations of a synthetic fixture, not a performance comparison.

## Known limitations of this evidence

- The provider is a local fixture, not a real model endpoint. No live-endpoint
  compatibility, quality, latency, or token-cost conclusion is supported.
- Both adapters are in-process; isolated CLI peak RSS and first-useful-edit
  time remain unmeasured (`null` in every observation).
- The fixture corpus used here is a one-file repository; the deterministic
  10,000-file reference fixture (`npm run benchmark:fixture`) has been
  validated for generation but has not been used for a paired measurement.
- The machine used is the spec §39 reference hardware (Mac mini M4 Pro 48 GB,
  macOS arm64), but the endpoint is the local fixture; reference-hardware
  performance targets against a real model endpoint remain unmeasured.

No performance, equivalence, or improvement claim is made by this report.
Live-endpoint paired measurement remains a release blocker tracked in
`docs/phase-7-evidence.md`.
